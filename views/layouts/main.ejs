<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Crowd Events' %></title>
    <link rel="stylesheet" href="/styles.css">
    <!-- Add any common CSS files here -->
</head>
<body>
    <header>
        <nav>
            <h1>Crowd Events Platform</h1>
            <% if (typeof isAuthenticated !== 'undefined' && isAuthenticated) { %>
                <p>Welcome, <%= user.name || user.email %>!</p>
                <a href="/logout">Logout</a>
            <% } else { %>
                <a href="/login">Login</a>
                <a href="/signup">Sign Up</a>
            <% } %>
        </nav>
    </header>

    <main>
        <%- body %>
    </main>

    <footer>
        <p>&copy; 2024 Crowd Events Platform. All rights reserved.</p>
    </footer>

    <script src="/script.js"></script>
    <!-- Add any common JavaScript files here -->
</body>
</html>